# @manasvi/session-sdk

Session and context lifecycle primitives for Manasvi:
- session store abstraction and in-memory implementation
- isolation-mode-aware session resolution
- structured context assembly pipeline
- provenance-preserving context chunk handling
- per-message context trace generation
